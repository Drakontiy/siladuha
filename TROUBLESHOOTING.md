# 🔧 Решение проблем с ботом

## Проблема: Бот не реагирует на /start

### Причины и решения:

### 1. ✅ Проверьте, что бот запущен

**Проверка:**
```bash
# Проверьте процессы Node.js
Get-Process node

# Или запустите бота заново
npm run dev:bot
```

**Должно быть видно:**
```
✅ Bot started successfully!
📱 Bot token: 8450371266...
🌐 MiniApp URL: https://...
✅ URL is valid for web_app: true/false
```

### 2. ✅ Проверьте токен бота в .env

**Проверка:**
- Откройте файл `.env`
- Убедитесь, что `BOT_TOKEN` указан правильно
- Токен должен быть в формате: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

**Как получить токен:**
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Скопируйте токен

### 3. ✅ Проверьте URL в .env

**Проверка:**
- URL должен начинаться с `https://` (не `http://`)
- Не должно быть слеша в конце:
  - ✅ Правильно: `https://example.com`
  - ❌ Неправильно: `https://example.com/`
- URL должен быть доступен (откройте в браузере)

**Правильный формат:**
```env
BOT_TOKEN=ваш_токен
MINIAPP_URL=https://your-server.com
PORT=3000
```

### 4. ✅ Перезапустите бота

**Важно:** После изменения `.env` нужно перезапустить бота!

1. Остановите бота: `Ctrl+C`
2. Запустите заново:
   ```bash
   npm run dev:bot
   ```

### 5. ✅ Проверьте логи

После отправки `/start` в Telegram, проверьте логи бота:

**Должно быть видно:**
```
📥 Received update: message
💬 Message: /start
🔍 Checking URL: https://... Valid: true/false
✅ Message sent with web_app button
```

**Если видите ошибку:**
- `BUTTON_URL_INVALID` - URL невалидный для web_app
- `Unauthorized` - неправильный токен
- `Bad Request` - проблема с форматом запроса

### 6. ✅ Проверьте, что используется правильный бот

**Проверка:**
1. Откройте Telegram
2. Найдите вашего бота по имени
3. Убедитесь, что это правильный бот (не другой с похожим именем)

### 7. ✅ Проверьте webhook

Если бот был настроен с webhook, нужно его удалить:

```bash
# Проверьте webhook
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo

# Удалите webhook (если установлен)
curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook
```

Затем перезапустите бота в режиме long polling.

### 8. ✅ Проверьте интернет-соединение

**Проверка:**
- Убедитесь, что компьютер подключен к интернету
- Проверьте, что нет блокировки Telegram API
- Попробуйте отправить запрос вручную:
  ```bash
  curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
  ```

## 🔍 Диагностика

### Запустите бота с подробными логами:

```bash
npm run dev:bot
```

**Что проверять в логах:**

1. **При запуске:**
   - ✅ `Bot started successfully!` - бот запустился
   - ✅ `Bot token: ...` - токен загружен
   - ✅ `MiniApp URL: ...` - URL загружен
   - ✅ `URL is valid for web_app: ...` - проверка URL

2. **При отправке /start:**
   - ✅ `Received update: message` - бот получил сообщение
   - ✅ `Message: /start` - команда распознана
   - ✅ `Checking URL: ... Valid: ...` - проверка URL
   - ✅ `Message sent with ...` - сообщение отправлено

3. **Если есть ошибки:**
   - ❌ `Bot error: ...` - ошибка бота
   - ❌ `Error sending web_app button: ...` - ошибка отправки
   - ❌ `Failed to start bot: ...` - ошибка запуска

## 📝 Пример правильной настройки

**Файл .env:**
```env
BOT_TOKEN=8450371266:AAGo6cPucnQlzcsNyGxqD0vGNcOzdbnKrgY
MINIAPP_URL=https://your-server.com
PORT=3000
```

**Запуск:**
```bash
npm run dev:bot
```

**Ожидаемый вывод:**
```
✅ Bot started successfully!
📱 Bot token: 8450371266...
🌐 MiniApp URL: https://your-server.com
✅ URL is valid for web_app: true
```

**В Telegram:**
1. Отправьте `/start`
2. Должен прийти ответ с кнопкой
3. Кнопка "🚀 Открыть Mini App" (если URL валидный)
4. Или "🔗 Открыть в браузере" (если URL невалидный)

## ✅ Если ничего не помогает

1. Проверьте все шаги выше
2. Убедитесь, что используете правильный токен
3. Проверьте, что бот не заблокирован
4. Попробуйте создать нового бота через @BotFather
5. Проверьте логи на наличие ошибок




