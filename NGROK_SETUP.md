# 📖 Подробная инструкция по настройке ngrok для Telegram Mini App

## Что такое ngrok?

ngrok - это инструмент, который создает безопасный туннель от публичного HTTPS URL к вашему локальному серверу. Это необходимо для работы Telegram Mini App, так как Telegram требует HTTPS URL.

## 🚀 Шаг 1: Регистрация на ngrok

1. Откройте сайт [ngrok.com](https://ngrok.com/)
2. Нажмите кнопку **"Sign up"** (Зарегистрироваться)
3. Зарегистрируйтесь с помощью:
   - Email и пароль
   - Или через Google/GitHub аккаунт
4. Подтвердите email (проверьте почту и перейдите по ссылке)

## 📥 Шаг 2: Установка ngrok

### Вариант A: Через npm (рекомендуется для проекта)

```bash
npm install -g ngrok
```

### Вариант B: Скачать вручную

1. Перейдите на [ngrok.com/download](https://ngrok.com/download)
2. Выберите вашу операционную систему:
   - **Windows**: скачайте `.zip` файл
   - **macOS**: скачайте `.zip` файл
   - **Linux**: скачайте `.tar` файл

3. Распакуйте архив в удобную папку (например, `C:\ngrok` или `~/ngrok`)

4. Добавьте ngrok в PATH:
   - **Windows**: 
     - Откройте "Переменные среды" → "Переменные Path"
     - Добавьте путь к папке с ngrok.exe
   - **macOS/Linux**: 
     ```bash
     sudo mv ngrok /usr/local/bin/
     ```

### Вариант C: Через Chocolatey (Windows)

```bash
choco install ngrok
```

### Вариант D: Через Homebrew (macOS)

```bash
brew install ngrok/ngrok/ngrok
```

## 🔑 Шаг 3: Получение токена авторизации

1. Войдите в [ngrok dashboard](https://dashboard.ngrok.com/)
2. Перейдите в раздел **"Your Authtoken"** (Ваш токен авторизации)
3. Скопируйте токен (он выглядит примерно так: `2abc123def456ghi789jkl012mno345pq`)

## ⚙️ Шаг 4: Настройка ngrok

Выполните команду для авторизации (замените `YOUR_AUTHTOKEN` на ваш токен):

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN
```

**Пример:**
```bash
ngrok config add-authtoken 2abc123def456ghi789jkl012mno345pq
```

После выполнения вы увидите сообщение:
```
Authtoken saved to configuration file: C:\Users\YourName\AppData\Local\ngrok\ngrok.yml
```

## 🧪 Шаг 5: Проверка установки

Проверьте, что ngrok установлен правильно:

```bash
ngrok version
```

Вы должны увидеть версию, например:
```
ngrok version 3.x.x
```

## 🚀 Шаг 6: Запуск ngrok туннеля

### Вариант A: Запуск в отдельном терминале (рекомендуется)

1. Откройте **новое** окно терминала/командной строки
2. Запустите команду (замените `3000` на порт вашего сервера, если он другой):

```bash
ngrok http 3000
```

**Важно:** Оставьте этот терминал открытым! ngrok должен работать всё время, пока вы используете бота.

3. Вы увидите вывод примерно такой:

```
ngrok                                                                   

Session Status                online
Account                       Your Name (Plan: Free)
Version                       3.x.x
Region                        United States (us)
Latency                       45ms
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:3000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

4. **Скопируйте HTTPS URL** из строки `Forwarding`:
   - В примере это: `https://abc123.ngrok-free.app`
   - **Этот URL будет меняться каждый раз при перезапуске ngrok!**

### Вариант B: Запуск с фиксированным доменом (платно)

Если у вас платная подписка ngrok, вы можете использовать фиксированный домен:

```bash
ngrok http 3000 --domain=your-fixed-domain.ngrok-free.app
```

### Вариант C: Запуск с настройками (продвинутый)

Создайте файл `ngrok.yml` в корне проекта:

```yaml
version: "2"
authtoken: YOUR_AUTHTOKEN
tunnels:
  webapp:
    addr: 3000
    proto: http
    # Для платного плана можно указать фиксированный домен:
    # domain: your-domain.ngrok-free.app
```

Затем запустите:

```bash
ngrok start webapp
```

## 📝 Шаг 7: Обновление .env файла

1. Откройте файл `.env` в корне проекта
2. Обновите `MINIAPP_URL` на ваш ngrok HTTPS URL:

```env
BOT_TOKEN=ваш_токен_бота
MINIAPP_URL=https://abc123.ngrok-free.app
PORT=3000
```

**Важно:** 
- Используйте HTTPS URL (начинается с `https://`)
- Не добавляйте слеш в конце (`/`)
- URL меняется при каждом перезапуске ngrok (если не используете фиксированный домен)

## 🔄 Шаг 8: Перезапуск бота

1. Остановите бота (если он запущен): нажмите `Ctrl+C` в терминале с ботом
2. Запустите бота заново:

```bash
npm run dev
```

3. Проверьте, что бот запустился без ошибок

## ✅ Шаг 9: Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Нажмите на кнопку "🚀 Открыть Mini App"
4. Mini App должно открыться в Telegram!

## 🎯 Шаг 10: Автоматизация (опционально)

Если вы хотите автоматически запускать ngrok вместе с ботом, можно использовать пакет `ngrok` в Node.js:

### Установка пакета:

```bash
npm install ngrok --save-dev
```

### Создайте файл `src/ngrok.ts`:

```typescript
import ngrok from 'ngrok';

export async function startNgrok(port: number): Promise<string> {
  try {
    const url = await ngrok.connect(port);
    console.log(`✅ ngrok tunnel: ${url}`);
    return url;
  } catch (error) {
    console.error('❌ ngrok error:', error);
    throw error;
  }
}

export async function stopNgrok(): Promise<void> {
  await ngrok.disconnect();
  await ngrok.kill();
}
```

### Обновите `src/index.ts`:

```typescript
import { startNgrok } from './ngrok';
import './bot';
import './server';

// Запуск ngrok для порта 3000
const NGROK_PORT = 3000;

startNgrok(NGROK_PORT).then(url => {
  console.log(`✅ Mini App URL: ${url}`);
  // Обновите MINIAPP_URL в .env на этот URL
}).catch(console.error);
```

## ⚠️ Важные замечания

### Бесплатный план ngrok:

- URL меняется при каждом перезапуске
- Ограничение на количество запросов
- Ограничение на время сессии (8 часов)
- Публичный URL виден всем

### Безопасность:

- Не делитесь ngrok URL публично
- Используйте HTTPS (ngrok предоставляет его автоматически)
- Для продакшена лучше использовать постоянный сервер с HTTPS

### Проблемы и решения:

1. **Ошибка "authtoken not found"**:
   ```bash
   ngrok config add-authtoken YOUR_AUTHTOKEN
   ```

2. **Порт уже занят**:
   - Проверьте, что порт 3000 свободен
   - Или используйте другой порт: `ngrok http 3001`

3. **URL не работает**:
   - Убедитесь, что ngrok запущен
   - Убедитесь, что сервер запущен на порту 3000
   - Проверьте .env файл

4. **Мини-приложение не открывается**:
   - Убедитесь, что URL в .env начинается с `https://`
   - Проверьте, что сервер отвечает (откройте URL в браузере)
   - Убедитесь, что перезапустили бота после изменения .env

5. **Ошибка ERR_NGROK_9040: "We do not allow agents to connect to ngrok from your IP address"**:
   
   Это означает, что ngrok блокирует подключение с вашего IP-адреса. Возможные причины и решения:
   
   **Причины:**
   - IP-адрес был заблокирован из-за нарушений правил использования
   - IP-адрес из региона, который заблокирован ngrok
   - Использование VPN/прокси, который заблокирован
   - Проблемы с провайдером (ISP)
   - Аккаунт ngrok ограничен или заблокирован
   
   **Решения:**
   
   **Решение 1: Сменить IP-адрес (VPN)**
   - Используйте VPN с IP-адресом из другой страны
   - Рекомендуемые VPN: NordVPN, ExpressVPN, Surfshark
   - Подключитесь к VPN серверу
   - Попробуйте снова запустить ngrok
   
   **Решение 2: Использовать другой VPN/Прокси**
   - Если уже используете VPN, попробуйте другой сервер
   - Или отключите VPN и попробуйте напрямую
   
   **Решение 3: Связаться с поддержкой ngrok**
   - Откройте [ngrok support](https://ngrok.com/support)
   - Создайте тикет с описанием проблемы
   - Укажите ваш IP-адрес и authtoken
   - Попросите разблокировать IP
   
   **Решение 4: Использовать альтернативные сервисы**
   - [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/) (бесплатно)
   - [Localtunnel](https://localtunnel.github.io/www/) (бесплатно)
   - [Serveo](https://serveo.net/) (бесплатно)
   - [localhost.run](https://localhost.run/) (бесплатно)
   - [Telebit](https://telebit.cloud/) (бесплатно)
   
   **Решение 5: Проверить аккаунт ngrok**
   - Войдите в [ngrok dashboard](https://dashboard.ngrok.com/)
   - Проверьте статус аккаунта
   - Убедитесь, что нет ограничений
   - Попробуйте создать новый аккаунт с другим email
   
   **Решение 6: Использовать другой регион ngrok**
   - Попробуйте указать другой регион:
   ```bash
   ngrok http 3000 --region=eu  # Европа
   ngrok http 3000 --region=ap  # Азия-Тихоокеанский регион
   ngrok http 3000 --region=au  # Австралия
   ngrok http 3000 --region=sa  # Южная Америка
   ngrok http 3000 --region=jp  # Япония
   ngrok http 3000 --region=in  # Индия
   ```
   
   **Рекомендация:**
   Если проблема сохраняется, лучше использовать альтернативные сервисы (Cloudflare Tunnel, Localtunnel) или обратиться в поддержку ngrok.

## 📚 Дополнительные ресурсы

- [Официальная документация ngrok](https://ngrok.com/docs)
- [ngrok Dashboard](https://dashboard.ngrok.com/)
- [Telegram Mini Apps документация](https://core.telegram.org/bots/webapps)

## 🎉 Готово!

Теперь у вас должен работать полноценный Telegram Mini App через ngrok! 🚀

