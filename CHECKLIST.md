# ✅ Чек-лист для проверки настроек Mini App

## 📋 Что нужно проверить после добавления HTTPS URL в .env:

### 1. ✅ Проверка .env файла

Убедитесь, что в `.env` файле:
- URL начинается с `https://` (не `http://`)
- Нет слеша в конце URL (правильно: `https://example.com`, неправильно: `https://example.com/`)
- URL указан без пробелов

**Правильный формат:**
```env
BOT_TOKEN=ваш_токен_бота
MINIAPP_URL=https://your-server.com
PORT=3000
```

### 2. ✅ Проверка доступности сервера

Откройте ваш HTTPS URL в браузере:
- URL должен открываться
- Должна загружаться страница мини-приложения
- Не должно быть ошибок SSL (сертификат должен быть валидным)

### 3. ✅ Перезапуск бота

**Важно:** После изменения `.env` файла нужно перезапустить бота!

1. Остановите бота: нажмите `Ctrl+C` в терминале
2. Запустите заново:
   ```bash
   npm run dev
   ```

### 4. ✅ Проверка работы бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Должна появиться кнопка "🚀 Открыть Mini App" (не "Открыть в браузере")
5. Нажмите на кнопку - Mini App должно открыться в Telegram

### 5. ✅ Проверка сервера на удаленном сервере

Убедитесь, что на удаленном сервере:
- Запущен сервер (Express) на правильном порту
- Сервер обслуживает статические файлы из папки `miniapp/dist`
- Мини-приложение собрано: `npm run build`
- Настроен HTTPS с валидным SSL сертификатом
- Сервер отвечает на запросы по HTTPS URL

### 6. ✅ Проверка CORS (если нужно)

Если мини-приложение не открывается, проверьте настройки CORS на сервере.

## 🔧 Если что-то не работает:

### Проблема: Кнопка "Открыть в браузере" вместо "Открыть Mini App"

**Решение:**
- Проверьте, что URL начинается с `https://`
- Убедитесь, что нет слеша в конце URL
- Перезапустите бота после изменения .env

### Проблема: Mini App не открывается

**Решение:**
- Проверьте, что сервер работает и доступен по HTTPS URL
- Откройте URL в браузере - должно загружаться мини-приложение
- Проверьте SSL сертификат - должен быть валидным
- Убедитесь, что мини-приложение собрано: `npm run build`

### Проблема: Ошибка при открытии Mini App

**Решение:**
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что все файлы мини-приложения загружаются
- Проверьте, что сервер правильно обслуживает статические файлы

## 📝 Пример правильной настройки:

**На удаленном сервере:**
```bash
# Сборка мини-приложения
npm run build

# Запуск сервера (если нужно)
# Сервер должен обслуживать файлы из miniapp/dist
```

**В .env файле:**
```env
BOT_TOKEN=8450371266:AAGo6cPucnQlzcsNyGxqD0vGNcOzdbnKrgY
MINIAPP_URL=https://your-server.com
PORT=3000
```

**Запуск бота:**
```bash
npm run dev
```

## ✅ Готово!

После всех проверок Mini App должно работать в Telegram! 🚀


