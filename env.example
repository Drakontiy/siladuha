# MAX Bot Token (получите в диалоге с Master Bot)
BOT_TOKEN=your_bot_token_here

# URL для Mini App (для запуска внутри MAX потребуется HTTPS)
# Для разработки используйте ngrok или cloudflare tunnel
# Пример: MINIAPP_URL=https://your-ngrok-url.ngrok.io
# Для продакшена используйте ссылку GitHub Pages
MINIAPP_URL=https://drakontiy.github.io/siladuha/
# Для локальной разработки можно раскомментировать строку ниже:
# MINIAPP_URL=http://localhost:3000

# Базовый URL API для синхронизации прогресса (используется мини-приложением)
# Для локальной разработки оставьте пустым — будет использован текущий origin
# Для продакшена укажите публичный адрес вашего сервера
# MINIAPP_API_BASE=https://siladuha.cloudpub.ru

# Порт для сервера (опционально, по умолчанию 3000)
PORT=3000

# Настройки подписки (если используете webhook вместо long polling)
# SUBSCRIPTION_URL=https://yourdomain.com/max-bot-webhook
# SUBSCRIPTION_SECRET=your_secure_secret_here


npm run build
node dist/index.js\


1. Я добавил в папку images hat.svg. Добавь во вкладку кастомизации новый заголовок Головные уборы. Перенеси награду за достижение первый шаг в под этот заголовок и сделай наградой за него эту шляпу. Она должна садиться ровно над огоньком в центре экрана
2. Во вкладке кастомизации замени фразу "открыть новый отттенок" на "открыть следующий уровень"
3. во вкладке время замени цвета кнопки "начать работу"
зелёный замени на голубой, который используется на линии для отображения отдыха. красный замени на цвет, используемый для отображения прокрастинации на линии.
4. во вкладке время убери возможность двигать линию кликом по ней. из-за этого не получается ставить метки. Выдели область, за котороую можно скроллить линию
5. во вкладке статистика подсвечивай выбранный сейчас интервал на календаре, когда пользователь меняет интервал
6. Сделай размер календаря статичным, чтобы стрелка для переключения месяца не двигалась при промотке