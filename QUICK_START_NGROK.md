# 🚀 Быстрый старт с ngrok (пошаговая инструкция)

## Шаг 1: Регистрация (2 минуты)

1. Откройте [ngrok.com](https://ngrok.com/)
2. Нажмите **"Sign up"** (Зарегистрироваться)
3. Зарегистрируйтесь (можно через Google/GitHub)
4. Подтвердите email

## Шаг 2: Установка через npm (1 минута)

Откройте терминал и выполните:

```bash
npm install -g ngrok
```

## Шаг 3: Получение токена (1 минута)

1. Откройте [ngrok dashboard](https://dashboard.ngrok.com/get-started/your-authtoken)
2. Скопируйте ваш **authtoken** (длинная строка символов)

## Шаг 4: Авторизация (30 секунд)

В терминале выполните (замените `YOUR_AUTHTOKEN` на ваш токен):

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN
```

**Пример:**
```bash
ngrok config add-authtoken 2abc123def456ghi789jkl012mno345pq
```

## Шаг 5: Запуск ngrok (30 секунд)

В **новом терминале** выполните:

```bash
ngrok http 3000
```

Вы увидите что-то вроде:

```
Forwarding  https://abc123.ngrok-free.app -> http://localhost:3000
```

**Скопируйте HTTPS URL:** `https://abc123.ngrok-free.app`

**⚠️ Важно:** Оставьте этот терминал открытым! ngrok должен работать всё время.

## Шаг 6: Обновление .env файла (1 минута)

1. Откройте файл `.env` в корне проекта
2. Обновите `MINIAPP_URL`:

```env
BOT_TOKEN=ваш_токен_бота
MINIAPP_URL=https://abc123.ngrok-free.app
PORT=3000
```

**Важно:** Используйте HTTPS URL (начинается с `https://`), без слеша в конце!

## Шаг 7: Перезапуск бота (30 секунд)

1. Остановите бота (если запущен): `Ctrl+C`
2. Запустите заново:

```bash
npm run dev
```

## Шаг 8: Проверка (1 минута)

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Нажмите кнопку "🚀 Открыть Mini App"
5. Mini App должно открыться! 🎉

## ✅ Готово!

Теперь у вас работает полноценный Telegram Mini App! 🚀

---

## 📝 Важные замечания

### URL меняется при перезапуске ngrok

Каждый раз при перезапуске `ngrok http 3000` вы получите новый URL. Поэтому нужно:
1. Скопировать новый URL
2. Обновить `.env` файл
3. Перезапустить бота

### Для постоянного URL (платно)

Если у вас платная подписка ngrok, используйте фиксированный домен:

```bash
ngrok http 3000 --domain=your-fixed-domain.ngrok-free.app
```

### Проблемы?

**Ошибка "authtoken not found":**
```bash
ngrok config add-authtoken YOUR_AUTHTOKEN
```

**Порт занят:**
- Проверьте, что порт 3000 свободен
- Или используйте другой порт: `ngrok http 3001`

**Mini App не открывается:**
- Убедитесь, что URL в `.env` начинается с `https://`
- Проверьте, что ngrok запущен
- Убедитесь, что перезапустили бота после изменения `.env`

---

📚 **Полная документация:** см. файл [NGROK_SETUP.md](NGROK_SETUP.md)


