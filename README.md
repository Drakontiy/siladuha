# Простой MAX бот с Mini App

Минимальный пример чат-бота для мессенджера MAX с кнопкой для открытия тестового мини-приложения.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка

Скопируйте `env.example` в `.env` и заполните:

```bash
cp env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=ваш_токен_бота
MINIAPP_URL=http://localhost:3000
MINIAPP_API_BASE=http://localhost:3000
```

> Для продакшена укажите публичный адрес мини-приложения, например:
> `MINIAPP_URL=https://drakontiy.github.io/siladuha/`
>
> А также установите `MINIAPP_API_BASE` на адрес своего backend-а (например, `https://siladuha.cloudpub.ru`), чтобы мини-приложение синхронизировало прогресс через REST API.

### 3. Получение токена бота

1. Откройте диалог с [Master Bot](https://max.ru/masterbot) в MAX
2. Следуйте инструкциям, чтобы создать нового бота
3. Скопируйте выданный токен в `.env`

### 4. Запуск

```bash
npm run dev
```

Скрипт поднимет два процесса:
- MAX-бота (long polling)
- Express-сервер для Mini App на порту 3000

## 📱 Использование

1. Найдите своего бота в MAX
2. Отправьте `/start`
3. Нажмите на кнопку "🚀 Открыть Mini App"
4. Откроется мини-приложение

## 📁 Структура проекта

```
bot-maxtg/
├── src/
│   ├── bot.ts          # MAX бот
│   ├── server.ts       # Express сервер для Mini App
│   └── index.ts        # Точка входа
├── miniapp/
│   ├── src/
│   │   ├── App.tsx     # React компонент Mini App
│   │   ├── App.css     # Стили
│   │   └── index.tsx   # Точка входа React
│   └── public/
│       └── index.html  # HTML шаблон
├── package.json
└── webpack.config.js
```

## 🔧 Доступные команды

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка проекта и обновление статики в `docs/` для GitHub Pages
- `npm start` - Запуск собранного проекта

## 🗄 Синхронизация прогресса

Прогресс и цели теперь хранятся на стороне сервера и привязываются к `user_id` в MAX. Мини-приложение загружает данные по API и синхронизирует изменения (с резервной копией в `localStorage`):

- `GET /api/user/:userId/state` — получить состояние пользователя;
- `POST /api/user/:userId/state` — сохранить состояние (данные активности + целевые показатели).

Все запросы выполняются на базовый URL из `MINIAPP_API_BASE` (если переменная не задана, используется текущий origin). Для продакшена убедитесь, что домен API доступен по HTTPS и разрешает CORS.

Каталог `src/data/` с пользовательскими JSON файлами исключён из git, но создаётся автоматически при запуске сервера. Его можно бэкапить или подключить любую другую СУБД, переопределив реализацию `userStateStore`.

## 🌐 Деплой на GitHub Pages

1. Выполните `npm run build` — скрипт соберёт бота и мини-приложение, а затем скопирует статику в папку `docs/`.
2. Закоммитьте изменения, в том числе содержимое `docs/`, и отправьте их в GitHub.
3. В настройках репозитория откройте `Settings → Pages` и выберите ветку `main`, папку `/docs`.
4. После публикации мини-приложение будет доступно по адресу [`https://drakontiy.github.io/siladuha/`](https://drakontiy.github.io/siladuha/).
5. Обновите `MINIAPP_URL` в `.env`, чтобы бот отдавал именно эту ссылку пользователям.

## 📝 Для продакшена

Для работы Mini App внутри MAX требуется HTTPS URL. Используйте:
- [ngrok](https://ngrok.com/) для туннеля
- [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/) (рекомендуется, если ngrok заблокирован)
- Или любой другой туннель/сервер с HTTPS

**Подробная инструкция по настройке ngrok:** см. файл [NGROK_SETUP.md](NGROK_SETUP.md)

**Альтернативы ngrok (если ERR_NGROK_9040):** см. файл [NGROK_ALTERNATIVES.md](NGROK_ALTERNATIVES.md)

### Быстрый старт с ngrok:

1. Зарегистрируйтесь на [ngrok.com](https://ngrok.com/)
2. Установите ngrok: `npm install -g ngrok`
3. Получите authtoken в [dashboard](https://dashboard.ngrok.com/)
4. Настройте: `ngrok config add-authtoken YOUR_AUTHTOKEN`
5. Запустите туннель: `ngrok http 3000`
6. Скопируйте HTTPS URL и обновите `MINIAPP_URL` в `.env`
7. Перезапустите бота

### Быстрый старт с Cloudflare Tunnel (если ngrok заблокирован):

1. Установите Cloudflared: `choco install cloudflared` (Windows) или `brew install cloudflared` (macOS)
2. Запустите туннель: `cloudflared tunnel --url http://localhost:3000`
3. Скопируйте HTTPS URL и обновите `MINIAPP_URL` в `.env`
4. Перезапустите бота

Обновите `MINIAPP_URL` в `.env` на HTTPS адрес, чтобы кнопка в MAX открывала мини-приложение без перехода в браузер.